import React from "react";
import { ActivityIndicator,TextInput,View, TouchableOpacity, Text ,Button} from "react-native";
;
// import {firebase,Auth} from '../App'
import firebase  from "firebase";
const LoadingScreen = (props) => {
   


function _loginwithPhone(){

}

  return (
    <View style={{backgroundColor:'gray',justifyContent:'center',alignItems:'center'}}>
    <ActivityIndicator size="large" color= "black" />
    
    </View>
  )
}

export default LoadingScreen;
